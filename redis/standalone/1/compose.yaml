version: "2"

services:
  redis-server:
    image: redis:${TAG:-alpine}
    container_name: redis-server
    hostname: redis-server
    network_mode: bridge
    ports:
      - 6379:6379
    environment:
      - TZ=Asia/Shanghai
      - MALLOC_ARENA_MAX=1
