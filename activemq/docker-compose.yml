version: "3"

services:
  activemq:
    build: .
    container_name: "activemq"
    ports:
      - 61616:61616
    volumes:
      - ./data/activemq/data:/data/activemq
      - ./data/activemq/log:/var/log/activemq
