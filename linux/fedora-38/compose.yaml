version: "2"

services:
  fedora:
    image: docker.io/fedora:38
    container_name: fedora-38
    network_mode: host
    tty: true
    stdin_open: true
    volumes:
      - shared:/root/shared
    environment:
      - TZ=Asia/Shanghai

volumes:
  shared:
    external: true
