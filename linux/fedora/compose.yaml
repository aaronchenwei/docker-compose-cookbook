version: "2"

services:
  fedora:
    image: docker.io/fedora:latest
    container_name: fedora
    network_mode: host
    tty: true
    stdin_open: true
    volumes:
      - shared:/root/shared
    environment:
      - TZ=Asia/Shanghai

volumes:
  shared:
    external: true
